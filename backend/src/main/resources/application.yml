spring:
  config:
    import: security/application-dev.yml #설정추가
  output:
    ansi:
      enabled: DETECT
logging:
  level:
    root: INFO
    org.apache: WARN
    org.springframework:
      orm:
        jpa: DEBUG
    org.springframework.boot.devtools: INFO
    org.kurento: INFO
    org.kurento.tutorial: INFO